import discord from "discord.js";
import { EvaluateOptions } from "ghom-eval";
export interface FormatOptions {
    ctx?: Record<string, any>;
    verbose?: boolean;
    muted?: boolean;
    lang?: EvaluateOptions["lang"];
}
export declare function evaluate(input: string, options?: FormatOptions, embedPresets?: {
    success: discord.EmbedBuilder;
    error: discord.EmbedBuilder;
}): Promise<discord.EmbedBuilder>;
