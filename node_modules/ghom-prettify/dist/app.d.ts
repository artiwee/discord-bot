import * as prettier from "prettier";
type Lang = "js" | "javascript" | "ts" | "typescript" | "md" | "markdown" | "yaml" | "css" | "scss" | "json" | "json5" | "html" | "vue" | "angular" | "php" | "xml";
/**
 * Format the given code. <br>
 * If the parser is not provided, it will be inferred from the language.
 */
export declare function format(code: string, options?: prettier.Options & {
    lang?: Lang;
}): Promise<string>;
export {};
