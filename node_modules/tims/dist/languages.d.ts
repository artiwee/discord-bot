import { Durations } from "./durations";
export interface LanguageTemplate {
    and: string;
    quantifiers: {
        one: string;
        nothing: string;
    };
    singular: Durations<string>;
    plural: Durations<string>;
}
export declare type Language = "fr" | "en" | "es";
export declare type Format = keyof typeof languages[Language]["plural"];
export declare const languages: {
    [key in Language]: LanguageTemplate;
};
